{
  "name": "packhelp-editor-ai-server",
  "version": "1.0.0",
  "private": true,
  "description": "Local backend proxy for editor_v1.html's AI brief step — keeps the Anthropic API key server-side, never in the static page.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
